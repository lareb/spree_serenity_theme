<%= form_for :order, :url => populate_orders_url, :remote => true, :html => {:class => "form-inline"} do |f| %>
  <div id="inside-product-cart-form" data-hook="inside_product_cart_form" itemprop="offers" itemscope itemtype="http://schema.org/Offer">
    <% if @product.price_in(current_currency) and !@product.price.nil? %>
      <div class="<% if !@product.has_variants? %> alpha <% else %> omega <% end %>">
        <div id="product_price" data-hook="product_price">
          <p class="price"><i class="icon-tag"></i><span class="price selling"><%= display_price(@product) %></span></p>
        </div>
        <div class="add-to-cart">
          <%= number_field_tag (@product.variants_and_option_values.any? ? :quantity : "variants[#{@product.master.id}]"),
            1, :class => 'title', :min => 1 %>
          <%= button_tag :class => 'theme', :id => 'add-to-cart-button', :type => :submit do %>
            <%= Spree.t(:add_to_cart) %>
          <% end %>
        </div>
      </div>
    <% else %>
        <div id="product-price">
          <br>
          <div><span class="price selling" itemprop="price"><%= Spree.t('product_not_available_in_this_currency') %></span></div>
        </div>
    <% end %>
  </div>
<% end %>

